# Test execution record template

**Not executed.** Copy to a dated file under `docs/manual-testing/executions/` after testing. Replace all fields; do not copy a template row into the summary as a result.

- Run ID / date and timezone / tester:
- Repository commit / target URL / target version if exposed:
- Browser and version / OS / network constraints:
- Account scenario / synthetic input data:
- Reset method and preconditions:

| Case ID | Actual observations | Result | Evidence | Defect or blocker |
| --- | --- | --- | --- | --- |
| Replace after execution | Record what happened, not just “as expected” | Not run | Pending | None assessed |

- Deviations from case steps:
- Retest of prior run (if applicable):
- Next action:

See [evidence rules](EVIDENCE_STANDARD.md) for result definitions and totals.
